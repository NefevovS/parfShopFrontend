import React from 'react';
import ProductList from "../components/ProductList";

const Catalog = () => {
    return (
        <div>
            catalog
            <ProductList/>
        </div>
    );
};

export default Catalog;